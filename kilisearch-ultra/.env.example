# Copy this file to .env and fill in real values. .env is gitignored and
# must never be committed or served — see storage/uploads/.htaccess-style
# denial rule for it in the app root .htaccess.
#
# List of configured connection profile names (comma-separated). Each name
# gets its own KILLI_DB_<NAME>_* block below.
KILLI_DB_PROFILES=

# Example profile named "default":
# KILLI_DB_DEFAULT_DRIVER=postgres
# KILLI_DB_DEFAULT_HOST=127.0.0.1
# KILLI_DB_DEFAULT_PORT=5432
# KILLI_DB_DEFAULT_DATABASE=
# KILLI_DB_DEFAULT_USERNAME=
# KILLI_DB_DEFAULT_PASSWORD=
# KILLI_DB_DEFAULT_SSL=false
